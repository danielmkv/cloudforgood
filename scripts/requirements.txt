# Core scientific stack
numpy>=1.26
xarray>=2024.1.0

# GFS GRIB2 ingestion
cfgrib>=0.9.12
eccodes>=1.6.0

# Contrail prediction
pycontrails>=0.50.0

# Polygon merging
shapely>=2.0.0

# AWS SDK (for uploading latest.geojson to S3)
boto3>=1.34.0
